# Environments (template)

> Curated topology. Fill in your own; keep secrets in `.env` (gitignored), never here.
> The machine-generated snapshot lives in `inventory/environments.generated.md`
> (gitignored) — regenerate with `bash scripts/refresh_inventory.sh`.

| Environment | Host | Notes |
|---|---|---|
| DEV | `<your-dev-host>` | … |
| PROD | `<your-prod-host>` | … |
